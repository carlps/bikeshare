TODO

Classes. AKA Models.
-- probably should just send data into model
-- and do all unpacking + MD5 in there.

system_regions
station_status
station_information


--should I add a transtype?
--would require checking deletes.
--probably worth it.
--more clear, less inference.
--can also try with a branch?