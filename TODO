TODO

sqlalchemy
    --convert to datetimes to datetime
    --bikeshare_etl:
        x-compare
        x-update_old
        x-load
        --metadata
    --cdc

X models all in one file models.py
    --can remove to_lists?
    --metadata class?
    --maybe a parent class (like Record?) that has base attributes? (to clean: if model==...)
        --maybe parent dimension or dim?
        -- I think I could do the parent, with some things let set_md5(md5_col), get_md5(md5_col) etc.


station status might be better for mongodb?

*args, **kwargs?


X-python 3.6

! unit tests

tests:
-insert on empty db
-compare IUD
-empty file
-views are correct
-all deletes?
-can get data and create obj

What happens when I, D, and then new? Probably a new I, right?


make a utils lib with things like:
    def backup_file(file):
        '''
        makes a copy of file with .bkp appended to filename in current working dir
        '''
        fname = os.path.join(os.getcwd(),file)
        fname_bkp = f'{fname}.bkp'
        return shutil.copyfile(fname,fname_bkp)