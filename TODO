TODO

Classes. AKA Models.
-- probably should just send data into model
-- and do all unpacking + MD5 in there.

system_regions
station_status
station_information

sqlalchemy?

--should I add a transtype?
--doing it in branch transtype
--need latest_row_indicator
--and view for latest non-deleted
--need to check for and flag Deletes